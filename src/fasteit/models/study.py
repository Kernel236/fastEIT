"""
Study dataclass — collection of sessions across patients.

TODO (Task 0.5.5): Implement Study dataclass with:
    sessions: list[Session]
    # High-level API:
    def from_folder(folder, config=None) -> Study: ...
    def to_dataframe() -> pd.DataFrame: ...   (all sessions, with patient_id)
"""
