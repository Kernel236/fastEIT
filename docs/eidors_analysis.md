# EIDORS Source Analysis

Notes from studying EIDORS MATLAB source code to understand the Dräger .eit format.

**IMPORTANT**: This document contains format understanding only — no EIDORS code is copied.
EIDORS is GPL. fasteit is Apache-2.0. These notes are original work.

---

## To be filled in (Task 2.0.x)

- Location of `read_draeger_header` in EIDORS source
- Location of `read_draeger_file` in EIDORS source
- Annotated fread calls: offset, type, n_values, meaning
- Comparison with hexdump findings

See also: `docs/reverse_eng/` for raw hexdump and string extraction outputs.
